Currently, Node.js seems to only work as a single process. This seems really inefficient on multi-core systems.

I tried spawning several instances of a server to work at the same time (node server.js &; node server.js), but that gives an error.
